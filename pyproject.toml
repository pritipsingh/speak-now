[project]
name = "agentos-docker"
version = "1.0.0"
description = "AgentOS: a production API for agents. Self-hosted Docker sibling of the agentos template family."
requires-python = ">=3.12,<4"
readme = "README.md"
license-files = ["LICENSE"]
authors = [{ name = "Agno", email = "hello@agno.com" }]

dependencies = [
  "agno[os,mcp,slack,pgvector,psycopg]==2.7.2",
  "openai",
  "parallel-web",
  "python-dotenv",
]

[project.optional-dependencies]
# ruff pinned so CI is deterministic (unpinned pulls the latest ruff, which changed
# formatting/lint rules and started reformatting Markdown code blocks).
dev = ["mypy", "ruff==0.12.2"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["agents*", "app*", "db*", "evals*", "workflows*"]
namespaces = false

[tool.ruff]
line-length = 120
exclude = [".venv*"]

[tool.ruff.lint.per-file-ignores]
"__init__.py" = ["F401", "F403"]

[tool.mypy]
check_untyped_defs = true
no_implicit_optional = true
warn_unused_configs = true
plugins = ["pydantic.mypy"]
exclude = [".venv*"]
