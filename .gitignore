# =========================
# Python
# =========================
__pycache__/
*.py[codz]
*$py.class

# Packaging / build artifacts
build/
dist/
*.egg-info/
.eggs/
*.egg

# Test / coverage
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.nox/

# Type checking / linting
.mypy_cache/
.dmypy.json
.pyre/
.pytype/
.ruff_cache/
.cache

# uv state — requirements.txt is the lockfile of record
uv.lock

# =========================
# Virtual envs + secrets
# =========================
.venv/
venv/
env/
ENV/
.env
.env.production
.envrc

# =========================
# Jupyter / notebooks
# =========================
.ipynb_checkpoints/

# =========================
# IDEs / editors
# =========================
.idea/
.vscode/

# =========================
# OS cruft
# =========================
.DS_Store
Thumbs.db

# =========================
# Project-specific
# =========================
tmp

# Claude Code: ignore local/personal state. The shared dev-time skills live in
# .agents/skills/ (committed); .claude/skills is a committed symlink into it —
# no trailing slash on the re-include, or it would only match a real directory.
# Add more re-include lines (e.g. !.claude/commands/) to ship other shared
# Claude config too.
.claude/*
!.claude/skills
!.claude/settings.json

# Other coding agents write their own config here (Codex, etc.) — keep it local.
# The shared, vendor-neutral assets live in .agents/skills/ and .mcp.json.
.codex/

# Node (local tooling installs for deploy scripts)
node_modules/
package.json
package-lock.json

# Optional: local PyPI creds (uncomment if you ever create it)
# .pypirc

# Speak — extra secret + build-artifact guards
.env.*
*.bak
*.bak.*
!example.env
mac-app/.build/
mac-app/*.app/
mac-app/*.dmg
compose.override.yaml
